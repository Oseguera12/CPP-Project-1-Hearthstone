character::character()
{
  // Base constructor for the character class
  health = 0;
  armor = 0;
  speed = 0
}

int character::character getHealth()
{
  // Returns the health from the base class
  return health;
}
